version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands: 
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - './*'
  name: deploy
  secondary-artifacts:
    artifact1:
      base-directory: dist/client
      files:
        - './**/*'
      name: priceNinjaClient
    artifact2:
      base-directory: dist/server
      files:
        - './**/*'
      name: priceNinjaServer



