version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands: 
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - './*'
    - 'dist/client/**/*'
    - 'dist/server/**/*'
  name: priceNinja-$(date +%Y-%m-%d)


